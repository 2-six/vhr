<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.vhr.mapper.UserMapper">

    <resultMap id="UserMap" type="com.example.vhr.entity.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="name" property="name"/>
        <result column="phone" property="phone"/>
        <result column="email" property="email"/>
        <result column="enabled" property="enabled"/>
        <collection property="roles" ofType="com.example.vhr.entity.Role">
            <id column="rid" property="id"/>
            <result column="rname" property="name"/>
            <result column="rnamezh" property="nameZh"/>
        </collection>
    </resultMap>

    <select id="loadUserByUsername" resultMap="UserMap">
        SELECT h.*, r.id rid, r.name rname, r.name_zh rnamezh
        FROM hr h
        LEFT JOIN hr_role hrr ON h.id = hrr.hr_id
        LEFT JOIN role r ON hrr.rid = r.id
        WHERE h.username = #{username}
    </select>

</mapper>